<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PageDeBase"
        tools:targetApi="31">
        <activity
            android:name=".equipe_creation_ou_rejoindre"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Add the following lines to declare equipe_rejoindre activity -->
        <activity
            android:name=".equipe_rejoindre"
            android:exported="true">
        </activity>
        <activity
            android:name=".equipe_creation"
            android:exported="true">
        </activity>
        <activity
            android:name=".equipe_rejoindre_info"
            android:exported="true">
        </activity>
        <activity
            android:name=".equipe_Admin"
            android:exported="true">
        </activity>
        <activity
            android:name=".equipe_base"
            android:exported="true">
        </activity>



    </application>

</manifest>
